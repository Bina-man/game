name: A++ Status Workflow

on: [push, pull_request]

jobs:
  build:
    name: A++ Build Status
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Run Tests
        run: echo "Running tests..."
      - name: Build Application
        run: echo "Building application..."
